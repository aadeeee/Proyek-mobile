import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

const Color primaryColor = Color(0xffFD61876E);
final NumberFormat rupiahFormat = NumberFormat.currency(
  locale: 'ID',
  symbol: "",
  decimalDigits: 0,
);
